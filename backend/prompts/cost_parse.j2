将记账描述解析为 JSON 对象。

【时间规则】
今天是 {{ current_date }}。如果用户未指定日期，默认使用今天。日期格式为 YYYY-MM-DD。
用户说的"昨天"对应 {{ yesterday }}，"前天"对应 {{ day_before_yesterday }}。

- record_type: "cost" 或 "income"
- category: 简短类别名，如"种子"、"化肥"、"人工"、"销售收入"
- amount: 数字字符串，不含单位
  - "w" 和 "万" 表示乘以 10000，例如 100w = 1000000
  - 如果换算后金额超过 10000000，amount 返回 "0"，并在 note 中说明需要用户确认
- record_date: YYYY-MM-DD 格式
- note: 备注。优先保存用户描述中的商品或事项，去掉日期、金额和记账动词；例如"今天买化肥100"返回"买化肥"。如果用户提到赊账、交易对象、用途或异常金额确认，也写入 note。

描述：{{ description }}
