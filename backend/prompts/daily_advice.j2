基于农场现状生成今天的农事建议，返回 JSON。

{% if cycle_id %}当前关注周期 ID={{ cycle_id }}。{% endif %}

{% if farm_context %}
{{ farm_context }}
{% endif %}

结构：
{
  "preview": "≤15字今日一句话总结",
  "items": [
    {
      "title": "≤10字结论",
      "detail": "≤40字原因和具体操作建议",
      "priority": 1到3（1=紧急，2=重要，3=提醒）,
      "icon": "一个emoji"
    }
  ]
}

- 最多 5 条，按 priority 升序。
- 只改写【今日行动候选】中的事项，不要自行扩展新建议。
- 无到期日欠款、未结人工、本月花费属于经营状态，不要写成今日建议。
- 天气风险必须给出可执行动作，例如排水、加固、错峰作业或暂停露天作业。
- 建议必须基于提供的数据；无种植计划时提醒创建种植周期。
- 避免重复近期已完成的操作。
