## 1. 数据模型与迁移

- [x] 1.1 为 `crop_cycles` 增加 MVP 批次字段，如总面积、季节和批次备注，并保持 `field_name` 兼容
- [x] 1.2 新增 `planting_units` 模型和迁移，支持批次下的棚、地块或区域
- [x] 1.3 新增 `operation_work_orders` 和 `operation_work_order_units` 模型和迁移，支持批次级和单元级作业范围
- [x] 1.4 新增 `workers` 和 `labor_entries` 模型和迁移，支持轻量工人档案和作业单用工明细
- [x] 1.5 设计人工成本与 `cost_records` 的 MVP 关联方式，并补充必要字段或关联表

## 2. 后端服务与 API

- [x] 2.1 扩展批次服务和 Schema，返回总面积、种植单元数量和当前阶段
- [x] 2.2 实现种植单元 CRUD 服务和 API，并限制在当前农场范围内访问
- [x] 2.3 实现作业单创建、列表、详情服务和 API，支持整个批次或多个种植单元作为作用范围
- [x] 2.4 实现西瓜内置作业类型和通用自定义作业类型查询接口
- [x] 2.5 实现工人档案 CRUD 服务和 API
- [x] 2.6 实现作业单用工明细创建和结算状态计算
- [x] 2.7 保存含用工的作业单时，自动生成或关联人工成本记录

## 3. 移动端 MVP 流程

- [x] 3.1 将种植规划列表升级为批次卡片，展示作物、品种、面积、单元数、阶段和状态
- [x] 3.2 在批次详情中展示种植单元列表，并支持新增和编辑种植单元
- [x] 3.3 新增作业单录入流程，支持选择批次、作用范围、作业类型、日期、备注和照片
- [x] 3.4 在西瓜批次下优先展示西瓜作业快捷项：压蔓、人工授粉、留瓜、垫瓜、采收、装车等
- [x] 3.5 在作业单录入中支持添加多个工人、计薪方式、数量、单价、已付金额
- [x] 3.6 在账单列表中标识来自作业单的人工成本，避免重复录入

## 4. Agent 与上下文

- [x] 4.1 扩展农场上下文摘要，包含活跃批次、近期作业单和未结人工摘要
- [x] 4.2 新增或扩展写操作 Skill，支持从自然语言创建作业单和用工明细
- [x] 4.3 为“今天东大棚 4 个工人给西瓜授粉，每人 200，先付老王 200”添加 pending action 确认流程
- [x] 4.4 确保 Agent 查询近期农事时合并新作业单和旧 `farm_logs`

## 5. 兼容与数据视图

- [x] 5.1 为旧 `crop_cycles.field_name` 提供兼容展示，并允许用户后续转为种植单元
- [x] 5.2 在近期农事、批次详情和报告数据中合并旧日志与新作业单
- [x] 5.3 确保利润统计包含作业单人工成本，不重复计算手工人工账单

## 6. 测试与验证

- [x] 6.1 为新增模型关系、农场隔离和迁移脚本添加后端测试
- [x] 6.2 为批次、种植单元、作业单、工人和用工 API 添加服务/API 测试
- [x] 6.3 为人工成本自动汇总和账单来源标识添加测试
- [x] 6.4 为 Agent 作业单写入和未结人工查询添加 Skill/上下文测试
- [x] 6.5 运行后端测试、前端测试和 harness 检查，记录 MVP 验证结果
