## 1. 数据模型与迁移

- [x] 1.1 为 `cost_records` 增加 `settled_amount` 和 `settlement_status` 字段，并补充模型、schema、迁移脚本
- [x] 1.2 迁移历史普通账单为已结清，历史未结赊账为未结，历史已结清赊账为已结清
- [x] 1.3 确保账单响应包含已结金额、未结金额和结算状态，且兼容旧客户端读取
- [x] 1.4 增加金额边界校验，禁止 `settled_amount` 小于 0 或大于 `amount`

## 2. 后端账单与赊账逻辑

- [x] 2.1 调整普通账单创建逻辑，未标记赊账/未收款时默认 `settled_amount=amount`、`settlement_status=settled`
- [x] 2.2 调整赊账/未收款创建逻辑，默认 `settled_amount=0`、`settlement_status=unsettled`
- [x] 2.3 重写还款/收款逻辑，使其更新原账单结算金额和状态，不再创建普通收入记录
- [x] 2.4 支持部分还款/收款，并返回剩余未结金额
- [x] 2.5 调整年度、月度、茬口利润和报表聚合，区分发生额、已结现金额和未结余额
- [x] 2.6 排除旧 `category=还款` 且带 `parent_record_id` 的记录对普通收入汇总的污染

## 3. 人工成本同步

- [x] 3.1 调整工资保存同步逻辑，使人工成本账单 `amount` 等于应付金额、`settled_amount` 等于已付金额
- [x] 3.2 调整工资更新和人工结算逻辑，确保关联成本账单结算状态与 `LaborEntry` 已付/未付一致
- [x] 3.3 保持同一工资来源只生成一条人工成本账单，重复提交不得增加发生支出
- [ ] 3.4 更新工人欠款摘要和茬口人工摘要，使其与账单结算口径一致

## 4. 移动端账单体验

- [x] 4.1 调整账单总览卡片，展示本月发生收入/支出、已收/已付、当前未收/未付
- [ ] 4.2 调整账单列表和详情，展示未结、部分结清、已结清标签和剩余金额
- [ ] 4.3 调整赊账筛选和入口文案，避免把未结账单显示成已付现金
- [ ] 4.4 调整记账页赊账/未收款创建参数，确保后端能识别结算状态
- [ ] 4.5 调整利润页和账单跳转，利润仍看发生额，账单页可核对已结和未结

## 5. 测试与验证

- [x] 5.1 增加后端测试：赊账支出计入发生支出但不计入已付支出
- [x] 5.2 增加后端测试：收入未收款计入发生收入但不计入已收收入
- [x] 5.3 增加后端测试：全额和部分还款更新原账单，不增加普通收入
- [x] 5.4 增加后端测试：人工工资应付、已付、未付同步到账单结算状态
- [x] 5.5 增加移动端测试或静态验证：账单总览和列表展示结算状态
- [ ] 5.6 运行 `ruff check . && ruff format .`、后端 pytest、移动端相关测试或类型检查
- [x] 5.7 记录任何既有非本变更问题，避免在本变更中混入无关重构
