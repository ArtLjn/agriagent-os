基于农场现状生成今天的农事建议，只返回 JSON，不要输出 Markdown。

{% if cycle_id %}当前关注周期 ID={{ cycle_id }}。{% endif %}

schema_version={{ schema_version | default("daily_advice_v2") }}
candidate_fingerprint={{ candidate_fingerprint | default("") }}

{% if farm_context %}
{{ farm_context }}
{% endif %}

【候选骨架】
{{ candidate_skeletons_json }}

{% if repair_instruction %}
【上次返回未通过校验，请严格修复】
{{ repair_instruction }}
{% endif %}

返回结构必须是：
{
  "preview": "≤20字今日一句话总结",
  "overview": {
    "score": 0到100,
    "subtitle": "首页经营态势摘要",
    "metrics": [
      {"key": "weather", "label": "天气", "value": "天气值", "level": "normal|important|urgent", "icon": "CloudSun"},
      {"key": "work_order", "label": "作业", "value": "作业数量", "level": "normal|important|urgent", "icon": "ClipboardList"},
      {"key": "pending", "label": "待处理", "value": "待处理数量", "level": "normal|important|urgent", "icon": "Bell"}
    ]
  },
  "items": [
    {
      "id": "必须来自候选骨架",
      "category": "必须与候选骨架一致",
      "level": "normal|important|urgent",
      "source_type": "必须与候选骨架一致",
      "source_id": "必须与候选骨架一致，可为 null",
      "priority": "必须与候选骨架一致，不能改高",
      "compact": {
        "title": "1到12字首页标题",
        "subtitle": "15到50字首页短说明",
        "icon": "沿用候选骨架图标",
        "icon_color": "沿用候选骨架颜色"
      },
      "detail_view": {
        "title": "详情标题",
        "description": "20到120字，说明原因和行动",
        "hero_badges": [],
        "evidence": [
          {"title": "依据标题", "description": "依据说明", "source_type": "同候选", "source_id": "同候选"}
        ],
        "steps": [
          {"order": 1, "title": "步骤标题", "description": "执行说明"},
          {"order": 2, "title": "步骤标题", "description": "执行说明"}
        ],
        "related": [],
        "actions": [{"type": "ask_agent", "label": "问问芽芽", "payload": {"candidate_id": "同 id"}}]
      }
    }
  ],
  "generation": {
    "schema_version": "daily_advice_v2",
    "mode": "llm",
    "retry_count": 0,
    "cache_hit": false,
    "candidate_fingerprint": "{{ candidate_fingerprint | default("") }}"
  },
  "created_at": "ISO8601 时间"
}

硬性规则：
- 只改写【候选骨架】中的文案字段，不要新增候选，不要输出候选外事项。
- 不要把无到期日欠款、未结人工、本月花费写成今日建议。
- 天气风险必须给出可执行动作，例如排水、加固、错峰作业或暂停露天作业。
- 每条 detail_view.actions 必须包含 type=ask_agent。
- items 最多 5 条，按 priority 升序。
