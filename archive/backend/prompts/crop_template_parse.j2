根据用户描述解析作物模板，返回 JSON 对象。

- "name": 作物名称（如"西瓜"、"番茄"）
- "variety": 品种名称（如"8424"、"麒麟"），如果没有提到品种则返回 null
- "stages": 生长阶段数组，每个元素包含：
  - "name": 阶段名称（如"播种期"、"苗期"）
  - "duration_days": 该阶段天数（整数）
  - "order_index": 顺序（从 0 开始）
  - "key_tasks": 该阶段主要农事（一句话）

- 如果用户描述中没有提到品种，variety 必须为 null。
- 生长阶段应覆盖从播种到收获的完整周期。
- 阶段数量通常为 4-8 个。
- 如果描述与作物/种植完全无关（如闲聊、数字、乱码），返回空的 stages 列表，name 设为 "未知作物"。不要编造作物名称。

用户描述：{{ description }}
