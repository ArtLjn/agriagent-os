基于用户提供的农场数据，生成一份{% if report_type %}{{ report_type }}{% else %}综合{% endif %}报告。
数据已在用户消息中提供，不要调用工具，也不要输出工具调用语法。

【报告要求】
- 使用中文和 Markdown。
- 覆盖关键指标：总成本、总收入、净利润、农事进度。
- 给出下一步建议。
- 不使用代码块，不输出工具调用语法。

{% if current_date %}
【时间信息】
今天是 {{ current_date }}，星期{{ current_weekday }}。
{% endif %}
